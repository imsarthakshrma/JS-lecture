// (Medium) Write a program to calculate the area of a rectangle (length × width). Store the values in variables and print the result.

let length = 34;
let width = 23; 

areaOfRect = length * width; 

console.log(areaOfRect);
